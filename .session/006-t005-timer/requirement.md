# 需求

## 任务 ID

T005 - 单任务固定间隔调度

## 本次承接范围

完整实现 T005，包括：

1. **定时调度器**：实现 timer.py，支持固定间隔调度
2. **生命周期管理**：浏览器保持运行，HTTP Session 每轮重建
3. **Ctrl+C 优雅退出**：当前轮完成后退出
4. **错误处理**：单轮失败不影响下一轮
5. **日志记录**：每轮执行状态

## 验收标准（来自冻结计划）

- 命令行参数：`python timer.py <配置路径> <间隔秒数>`
- 首轮立即执行，后续按固定间隔
- 浏览器类任务保持 Page，API 类任务每轮重建 Session
- 浏览器断连或 Tab 关闭时退出
- Ctrl+C 在当前轮完成后退出（不中断执行中的模板）
- 单轮失败记录日志，继续下一轮
- 真实验证：运行多轮、中途中断、单轮失败恢复

## 依赖状态

- T001 ✅ 已完成：executor.py、shared/config.py、shared/output.py 已就绪
- T002 ✅ 已完成：API 模板实现完成
- T004 ✅ 已完成：浏览器模板实现完成

## 实施约束

- 在 T004 后实现 timer.py，不修改共享文件
- 按冻结架构实现生命周期管理
- 遵守共享文件修改顺序（T001 -> T002 -> T003 -> T004 -> T005）
- 复用 executor.py 的执行逻辑
