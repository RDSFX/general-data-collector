# 需求

## 任务 ID

T001 - 配置、执行入口和历史输出

## 本次承接范围

完整实现 T001，包括：

1. **依赖声明**：创建 requirements.txt，声明 requests、playwright 及版本
2. **配置读取与校验**：实现 shared/config.py，支持 UTF-8 TOML 读取、字段/路径/模板名称/任务名称校验
3. **历史输出**：实现 shared/output.py，支持 JSONL 追加、日期分文件、任务隔离、日志配置
4. **执行入口**：实现 executor.py，支持单配置参数、一次执行、模板映射、错误分类、统一落盘

## 验收标准（来自冻结计划）

- UTF-8 配置正确读取
- 非法字段/路径及多个配置参数被拒绝
- 用测试桩验证追加、日期分文件、写入失败退出及日志脱敏
- 模板未实现时明确报错，不提供假成功结果

## 实施约束

- 按冻结架构实现接口，为后续 T002-T006 建立公共基础
- 模板当前使用桩实现，返回测试数据以验证执行链路
- 环境已核实：Python 3.11.5、.venv 已存在、Chrome 已安装
