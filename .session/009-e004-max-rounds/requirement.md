# 需求

**会话编号**：009  
**关联任务**：E-004（总采集次数限制）  
**日期**：2026-09-24

## 任务范围

实施 [docs/future-enhancements.md](../../docs/future-enhancements.md#e-004--总采集次数限制) E-004：为 timer.py 添加总采集次数限制，支持"采集 N 次后自动停止"。

**实施方案**：
- 配置方式：方案 B（配置文件 `[schedule]` 新增 `max_rounds`）
- 默认行为：不填时无限循环（保持当前行为）
- 计数语义：所有轮次（包括失败）都计入次数
- 达到次数后输出日志并退出

## 本次范围

1. 修改 [timer.py](../../timer.py)，支持从配置读取 `schedule.max_rounds`（可选字段，正整数）
2. 循环控制：达到指定轮次后退出
3. `max_rounds` 缺失或 ≤ 0 时无限循环（向后兼容）
4. 达到限制时输出清晰日志
5. 更新相关文档
