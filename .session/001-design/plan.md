# 会话方案

1. 已完成：逐项确认四类采集、单配置单进程、间隔调度、专属 Tab、登录、错误和数据保存行为。
2. 已完成：只读核对 Python、虚拟环境及 Chrome，整理 ENV.md。
3. 已完成：确认依赖选型、三类 10 秒超时、路径/时间/任务名及浏览器配置。
4. 已完成：确认四类 TOML、模板/JS 接口、固定输出目录。
5. 已完成：用户确认 T001-T006 任务边界与验收，并明确准许冻结当前设计。
6. 已完成：冻结架构、配置接口和计划，初始化实施任务为 todo，补齐决策/交接，001 标记 done。

## 验证方法

完成文档状态、需求覆盖、任务依赖、配置示例与引用的检查。环境探测未安装软件或启动采集，业务功能检查留给实施任务。
