# {{项目名}}

{{项目简介}}

## 基本约定

- 使用中文沟通，保留代码标识符和命令原名。记录简明的事实、结论和待办，避免重复。
- 读写文件显式使用 UTF-8：PowerShell 指定 `-Encoding UTF8`，Python 指定 `encoding="utf-8"`。
- 修改前阅读相关文件及适用的 `CLAUDE.md`，沿用现有风格，保留用户已有改动；使用 Git 时先检查工作区状态。
- 纯问答和只读浏览无需创建会话记录。

## 开始与续接

1. 阅读 `README.md`、`.session/INDEX.md` 和 `docs/task-progress.md`，确定当前阶段与待办。
2. 续接任务时阅读相关交接；实施前补读 `ENV.md`、架构和任务计划，核对环境、范围及依赖。
3. 优先续接未完成任务，否则选择依赖已满足的任务；不抢占其他会话正在处理的任务。
4. 需要建档时复用当前会话，或复制 `.session/_template/` 创建 `NNN-short-slug` 并登记索引。编号从 `001` 开始递增，忽略 `_template/`；设计未完成时继续 `001`。

## 001：设计与计划

本阶段完成设计，不开展业务实现。

1. 澄清需求、范围、约束和验收标准；能从项目中确认的信息直接读取，关键未知项向用户核实。
2. 在根目录生成 `ENV.md`，记录本地运行环境、使用方式和待准备项；不涉及运行环境时简述原因。
3. 编写 `docs/architecture.md` 与 `docs/task-plan.md`：明确模块、接口、任务边界、依赖及验收方法。任务尽量可独立验证，共享修改写清归属和顺序，必要时单列集成任务。
4. 与用户确认方案，解决影响实施的关键问题，将两份文件设为 `frozen`，记录日期与来源会话；从计划初始化任务进度后，将 `001` 标记为 `done`。

未完成时保持 `draft`，在任务进度中记录剩余设计项。

## 002 及之后：实施

1. **核对条件**：确认设计已完成、依赖已交付、环境可用；存在影响本任务的问题时先处理或记录阻塞。
2. **登记范围**：默认一个会话承接一个任务，可跨会话完成。在 `requirement.md` 记录任务 ID 和本次范围，在 `plan.md` 写必要步骤，并更新任务进度与承接会话。
3. **实施与验证**：按计划修改，运行与改动匹配的检查，在 `procedure.md` 记录关键结果。纯文档修改无需引入测试框架。
4. **更新进度**：同步已完成内容、剩余工作和阻塞；满足验收标准且有验证依据后，任务才能标记为 `done`。

`frozen` 表示已确认的基线，不随日常进度改写。范围、接口或验收标准需要变更时，先说明影响并取得用户确认，再同步相关文档和进度，记录变更原因；不擅自扩大任务。

## 命令被拒绝时

1. 命令执行被权限或审批机制拒绝时，停止该操作，不重试、不提权，也不更换工具或改写命令尝试同一操作。
2. 继续完成不依赖该操作的剩余工作，记录被拒绝的命令、原因及未验证内容。
3. 完成其余工作后，向用户列出需手动执行的命令和操作，注明执行目录、必要前提、验证目的及通过标准。
4. 明确请用户执行后将结果粘贴回复；收到结果前，相关验证保持未完成，不宣称通过。

## 收尾与交接

用户要求“收尾”“归集”“归档”或“交接”时，立即整理当前成果，不为补齐任务继续开发或启动长时间验证。

1. 补齐 `procedure.md` 的实际变更和验证结果，在 `summary.md` 概括成果、未验证项与下一步。
2. 更新任务进度：状态、剩余动作及完成条件、阻塞、验证依据和交接链接；清空当前承接会话。
3. 有可复用知识或重要决策时写入 `docs/decisions.md`，无需为无新增内容的栏目补写记录。
4. 同步会话需求元数据与索引：已验收设为 `done`，已交接但仍有工作设为 `archived`。任务状态按实际完成情况保留。
5. 告知用户交接位置、当前状态和下一步，结束本次工作。

`001` 提前收尾也遵循上述步骤，保留草案并记录剩余设计项，后续继续设计。

## 文件与状态

- `docs/architecture.md`、`docs/task-plan.md`：已确认的设计与任务范围。
- `docs/task-progress.md`：执行状态、剩余工作与交接的统一入口。
- `ENV.md`：运行环境说明，环境变化时核实并更新。
- `docs/decisions.md`：重要决策和可复用知识；`.session/`：会话记录；`src/`：源码。
- 任务编号使用 `T001` 等稳定 ID；需要拆分时使用 `T001.01` 等子任务 ID。
- 任务状态：`todo`、`in_progress`、`blocked`、`done`。
- 会话状态：`open`、`done`、`archived`、`abandoned`。恢复时改为 `open`；会话归档不代表任务完成。

## 项目命令

按实际工具填写，未配置项不可直接执行。

| 用途 | 命令 |
|------|------|
| 安装依赖 | 待配置 |
| 本地启动 | 待配置 |
| 测试 | 待配置 |
| 格式 / 静态检查 | 待配置 |
| 构建 | 待配置 |

交付时说明关键变更、验证结果和剩余工作；未执行或失败的检查如实说明。
